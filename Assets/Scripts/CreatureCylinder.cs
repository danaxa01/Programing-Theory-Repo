using UnityEngine;

public class CreatureCylinder : Creature
{
    // Fall down and roll in a direction
    protected override void Move()
    {
        return;
    }

    // Fall down and roll over the attack target and jump on it
    // side note: maybe spawn ramp to do a flip?
    protected override void Attack()
    {
        return;
    }

    protected override void EnterAnimation()
    {
        return;
    }
}
